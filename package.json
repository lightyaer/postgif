{
  "name": "postgif",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "app"
  ],
  "scripts": {
    "app:dev": "yarn workspace app dev",
    "app:build": "yarn workspace app build",
    "app:preview": "yarn workspace app preview",
    "test": "node ./tests/postgif.js"
  },
  "dependencies": {
    "@project-serum/anchor": "^0.19.0"
  },
  "devDependencies": {
    "chai": "^4.3.4",
    "mocha": "^9.0.3",
    "prettier": "^2.5.1"
  }
}
